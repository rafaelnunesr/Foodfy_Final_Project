{% macro render(title, classes) %}
    <section class='card {{classes}}'>

        <div class="card-header">
            <h1>{{title}}</h1>
        </div>

        <div class="card-container">
            {{ caller() }}
        </div>

    </section>
{% endmacro %}